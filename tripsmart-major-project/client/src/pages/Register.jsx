import { useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import Navbar from '../components/Navbar'; import api from '../api/api'; import { useAuth } from '../context/AuthContext';
export default function Register(){ const [form,setForm]=useState({name:'',email:'',password:''}); const nav=useNavigate(); const {login}=useAuth(); async function submit(e){e.preventDefault(); const {data}=await api.post('/auth/register',form); login(data); nav('/dashboard');} return <><Navbar/><div className="max-w-md mx-auto py-12"><form onSubmit={submit} className="card space-y-4"><h1 className="text-3xl font-bold">Create Account</h1><input className="input" placeholder="Name" onChange={e=>setForm({...form,name:e.target.value})}/><input className="input" placeholder="Email" onChange={e=>setForm({...form,email:e.target.value})}/><input className="input" type="password" placeholder="Password" onChange={e=>setForm({...form,password:e.target.value})}/><button className="btn w-full">Register</button><p>Already registered? <Link className="text-orange-600" to="/login">Login</Link></p></form></div></> }
